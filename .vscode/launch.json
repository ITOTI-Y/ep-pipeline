{
    "configurations": [
        {
            "name": "pytest",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/tests",
            "console": "integratedTerminal",
            "args": "pytest"
        }
    ]
}